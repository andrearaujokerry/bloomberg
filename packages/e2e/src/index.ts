// WP-01 scaffold placeholder — the real suite lives in `tests/**` alongside
// `playwright.config.ts` and `fixtures/serverProcess.ts`. It exists only so that `tsc -b` has a
// compilable root for this project.
export const PACKAGE_NAME = '@terminal/e2e' as const;
