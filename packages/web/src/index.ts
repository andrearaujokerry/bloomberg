// WP-01 scaffold placeholder — the real entry point is `src/main.tsx` (vite) and the barrel is
// written by the UI work packages. It exists only so that `tsc -b` has a compilable root here.
export const PACKAGE_NAME = '@terminal/web' as const;
